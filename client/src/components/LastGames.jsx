import React, { useEffect } from "react";

function LastGames({ games }) {
  return (
    <div>
      {games.map((game) => (
        <h2>hey</h2>
      ))}
    </div>
  );
}

export default LastGames;
